import React from 'react';

const Mobile = () => {
  return (
    <div className="mobile_container">
      <span className="mobile_text">Mobile browsers are currently unsupported {':('}</span>
    </div>
  );
}

export default Mobile;